[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "logtrace-sdk"
version = "0.1.0"
description = "Python SDK for the Logtrace developer API"
requires-python = ">=3.9"
license = "MIT"
